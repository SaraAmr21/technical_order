<odoo>

    <record id="view_rejection_reason_wizard_form" model="ir.ui.view">
        <field name="name">rejection.reason.wizard.form</field>
        <field name="model">rejection.reason.wizard</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>

                        <group>
                            <field name="reason_cancellation"/>
                        </group>
                    </group>

                </sheet>
                <footer>
                    <button name="action_confirm" type="object" string="Confirm" class="btn-primary"
                            data-hotkey="q"/>
                    <button name= "action_cancel" type="object" special="cancel" data-hotkey="z" string="Cancel" class=" btn-secondary"
                    />
                </footer>
            </form>
        </field>
    </record>
    <record id="action_rejection_reason" model="ir.actions.act_window">
        <field name="name"> rejection.reason.action</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">rejection.reason.wizard</field>
        <field name="view_mode">form</field>
        <field name="context">{}</field>
        <field name="target">new</field>

    </record>


</odoo>
